import 'package:flutter/material.dart';

class LiveTVPage extends StatelessWidget {
  const LiveTVPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Live TV Page'),
   );  }
}
